# HANDOFF — Claude Code ↔ Antigravity Co-Work

**작성:** 2026-07-25 · **작성자:** Claude Code (Opus 5) & Antigravity (Google DeepMind) · **대상:** `/Users/owen/WWF`  
**상태:** ✅ **[점검 완료]** TypeScript 정적 타입검사 (`npx tsc --noEmit`) 0에러, 프로덕션 빌드 (`npm run build`) 통과.  
**개발 노트:** 상세 협업 히스토리 및 개발 가이드는 [`DEVLOG.md`](file:///Users/owen/WWF/DEVLOG.md) 참조.

---


# [12차] K. Surfline 520표본 대조 — 척도 보정

## K.1 왜 했나

"물치가 8점은 너무 낮은 거 아니냐" + "다른 앱들의 점수도 비교평가해보라".
[10~11차] 재설계가 **과교정**됐는지 외부 기준으로 확인이 필요했습니다.

## K.2 Surfline 공개 API 로 실제 등급 확보

```
kbyg/spots/forecasts/rating?spotId=…&days=5&intervalHours=3
kbyg/spots/forecasts/surf?spotId=…       ← 쇄파 파고 + humanRelation
```

한국 13개 스팟 × 5일 × 3시간 = **520표본**. 등급별 평균 조건:

| Surfline | n | 너울 | 주기 | 풍파 | 바람 | 방위 어긋남 |
|---|---|---|---|---|---|---|
| 0 VERY_POOR | 105 | 0.46m | 4.88s | 0.62m | 22.0 km/h | 89° |
| 1 POOR | 215 | 0.50m | 4.74s | 0.26m | 15.1 km/h | 50° |
| 2 POOR_TO_FAIR | 153 | 0.64m | 4.87s | 0.12m | 13.2 km/h | 23° |
| 3 FAIR | 47 | 0.66m | 5.14s | 0.09m | 10.0 km/h | 24° |

**주기는 전 구간 4.7~5.1초로 거의 안 움직입니다.** 한국 여름에는 Surfline 도
주기로 등급을 가르지 않고 **바람·방위·풍파**로 가릅니다.

## K.3 대조 결과 — 순위는 맞고 척도가 압축돼 있었습니다

```
같은 조건을 우리 엔진에 넣으면
  우리 : 2 → 9 → 20 → 24
  SL   : 0 → 14 → 29 → 43   (v/7×100 환산)
```

단조 증가로 **순위는 완벽히 일치**. 다만 상단에서 뭉갰습니다.
원인: 우리 바람 계수가 "오프쇼어 18km/h 이하 = 전부 1.0" 인 **계단**이라,
Surfline 이 실제로 2↔3 을 가르는 구간(13.2 → 10.0km/h)에서 아무 차이도
내지 못했습니다. → **풍속 연속 감쇠**로 교체(0km/h 1.15 → 40km/h 0.68).

## K.4 🔴 그리고 판정 구간이 죽어 있었습니다

곱셈 엔진으로 갈아엎고도 예전 임계값(좋음 60 / 평범 38)을 그대로 뒀더니,
한국 전역 13스팟 × 6일 낮시간 **1456표본에서**:

```
좋음 0.0%  ·  평범 1.9%  ·  나쁨 98.1%
```

**DEVLOG [2차] §D 가 이미 한 번 고쳤던 실패의 재발입니다**
("동해안 여름은 예보가 전부 플랫로 나와 어느 날 갈까를 판단할 수 없었다").

### 임계값을 어디에 맞출 것인가

이번 주(여름) 분포의 백분위로 자르면 **겨울이 전부 좋음**이 됩니다.
그래서 **연중 캘리브레이션이 된 Surfline 의 등급 경계**에 맞췄습니다.

```
평범 ≥ 18   (Surfline POOR_TO_FAIR 경계 = 우리 20점)
좋음 ≥ 45
```

결과 분포: **좋음 0.7% · 평범 16.6% · 나쁨 82.7%** (여름 기준).
여름에 좋음이 0.7% 인 건 정직합니다 — 한국 여름은 태풍 말고는 실제로 없습니다.
평범 16.6% 라 "이번 주 그나마 나은 시간대" 를 고를 수 있습니다.

## K.5 최종 상태

| | 8/30 08시 | 판정 |
|---|---|---|
| 천진 | 4 | 나쁨 (장판 — swellWindow 차폐) |
| 봉수대 | 26 | 평범 |
| 설악 | 21 | 평범 |
| 물치 | 21 | 평범 ("그럭저럭" 과 부합) |

회귀: `0.6m/7s 44 FAIR ○` · `1.0m/8s 70 ●` · `1.2m/11s 90 ●` · `1.5m/12s 100 ●`

### 남은 한계 (해결 안 됨, 기록만)

봉수대(26)가 물치(21)보다 높습니다. 현장에서는 봉수대가 더 나빴습니다.
둘은 **다른 격자**지만 봉수대 쪽 너울이 0.1m 더 큰 게 그대로 반영된 결과입니다.
5점 차이를 맞추려고 상수를 만지는 건 관측 1회 과적합이라 **하지 않았습니다.**
설악·물치(21, 21)는 아예 같은 격자라 원리적으로 구분 불가입니다.

### 참고 — Surfline 도 천진을 구분하지 못했습니다

8/30 천진은 완전한 장판이었는데 Surfline 은 이웃과 똑같이 POOR_TO_FAIR 를 줬습니다.
LOTUS 의 bathymetry 로도 못 잡은 로컬 차폐를, 우리는 현장 관측 1회로 `swellWindow`
를 넣어 잡고 있습니다. **더 정확할 수도, 과적합일 수도 있습니다** — 반증이 나오면
되돌리세요(data/fieldReports.ts).

---


# [11차] J. 급경사도 항 추가 · 실측 기록의 인식론 정정

## J.1 🔴 관측을 숫자로 바꿔 놓고 그 숫자로 채점했습니다

[10차] I 에서 `fieldReports.ts` 에 `observedScore: 0 / 10 / 20 / 40` 을 넣고
README 에 "실측" 이라는 표까지 만들었습니다. **40 을 뺀 나머지는 제가 사용자의
서술을 보고 임의로 매긴 숫자입니다.** 그리고 그 숫자로 모델을 검증했습니다.
자기가 만든 답안지로 채점한 셈입니다.

40 조차 "현장 로컬 1인이 구두로 100점 중 40점이라고 함" 이지 측정값이 아닙니다.
척도가 우리 점수와 같다는 근거도 없습니다.

**→ `observedScore` 필드를 없애고 `observedScoreNote`(출처 명시 문자열)로 교체.**
서술(observed)이 관측이고 숫자는 해석이라는 경고를 파일 헤더에 박았습니다.

## J.2 봉수대가 설악보다 높았던 건 물리적으로 거꾸로였습니다

| | 너울 | 이전 점수 | 현장 |
|---|---|---|---|
| 봉수대 | 1.0m / 4.85s | 17 | 코앞 붕괴, 될까 싶은 수준 |
| 설악 | 0.9m / 4.75s | 14 | 챠피·지저분 |

봉수대는 파고만 더 큰데 점수가 더 높았습니다. 그런데 **짧은 주기에서는 파고가 클수록
더 심하게 클로즈아웃**됩니다. 크기 점수만 있으면 이 역전을 표현할 수 없습니다.

### 파형 급경사도 (wave steepness)

```
급경사도 = H₀ / L₀ = H₀ / (1.56·T²)
```

그라운드 스웰은 대체로 0.005 이하, 풍파는 0.02 를 넘습니다.
0.006→1.0, 0.018→0.82, 0.024→0.62, 0.030→0.45, 0.040→0.3 로 보간해 곱합니다.

**효과 — 같은 주기에서 파고만 키울 때:**

```
 5초 : 0.6m 12점 → 1.2m 10점 → 2.0m  8점   (커질수록 나빠짐)
12초 : 0.6m 54점 → 1.2m 85점 → 2.0m 100점  (커질수록 좋아짐)
```

이게 "크지만 못 타는 파도" 의 정체이고, surf-forecast 의 Open star 가 잡아내려는 것과
같은 현상입니다.

## J.3 재검증

| 스팟 | I 차 | **J 차** | 현장 서술 |
|---|---|---|---|
| 천진 | 3 | **1** | 파도 한 점 없는 장판 |
| 봉수대 | 17 | **9** | 코앞 붕괴 |
| 설악 | 14 | **8** | 챠피·지저분 |
| 물치 | 14 | **8** | 그럭저럭 (가본 곳 중 최선) |

회귀 이상 없음: `1.2m/11s 85 EPIC ●` · `1.5m/12s 100 EPIC ●` · `0.6m/7s 39 FAIR ○`

설악·물치가 8 로 같은 건 **같은 9km 격자라 구분할 정보가 없다**는 뜻입니다.
억지로 벌리지 않고 그대로 둡니다.

---


# [10차] I. 🔴 스코어 엔진 전면 재설계 — 현장 실측이 잡아낸 구조적 오류

## I.1 무엇이 드러났나

사용자가 2026-08-30 고성·속초·양양 4곳을 직접 다녀왔습니다.

| 스팟 | 앱 | 현장 |
|---|---|---|
| 천진 | **80** | 파도 한 점 없는 장판 (해수욕장 개장 중) |
| 봉수대 | **78** | 해변 코앞에서 컬 → 즉시 붕괴, 세트가 너무 잦게 연속 |
| 설악 | **72** | 매우 챠피하고 지저분 |
| 물치 | **70** | 그럭저럭 (로컬 평가 40/100) |

그날 데이터를 뜯어보니 **너울 주기가 4.75~4.85초**였습니다. 원인은 둘이었습니다.

1. **엔진이 `wave_period`(전체 6.5초)를 썼습니다.** `swell_wave_period`(4.8초)를 봐야 했습니다.
   전체값에는 풍파가 섞여 있어 못 타는 파도가 탈 만해 보입니다.
2. **바람이 최대 35점을 독립 가산했습니다.** 그래서 **탈 파도가 없어도 글래시이기만 하면
   80점**이 나왔습니다. 천진이 정확히 그 경우 — 사용자 표현 "해수욕으로는 최고인 평화로운 바다".

## I.2 지식 기반 재설계

노트북(`nlm notebook query 6f3f58fd-…`)에서 예보 방법론을 받아 근거를 잡았습니다.

- **파장** `L₀ = 1.56T²`, 해저 감지수심 `L₀/2`
  → 5초는 수심 20m 에서야 지형을 느낌(코앞 붕괴), 10초는 78m 부터(정렬되어 도착)
- **그라운드 스웰 경계 = 10초.** 그 미만은 윈드 스웰
- **관행 가중치**: 에너지 `P∝H²T` 50~60% · 바람 30% **곱셈** · 조석 10~15% **스팟별 게이트**
- **온쇼어 15km/h↑ → 배율 0.2~0.4**
- **surf-forecast Solid/Open star**: Solid = 주기 10초+ AND (오프쇼어 or 무풍 5km/h↓)

### 새 구조 — 곱셈

```
점수 = 크기(너울파고) × 주기계수 × 방위 × 차폐 × 바람 × 지저분함 × 조석
```

한 축이 0이면 전체가 0입니다. 서핑이 그렇습니다.

### 함께 살아난 것들

- **`spot.tidePreference` 가 드디어 점수에 쓰입니다.** 그동안 필드만 있고
  전역 규칙("간조 -5 / 들물 +3")으로 서해 만리포와 동해 죽도를 똑같이 다뤘습니다.
  이제 스팟 선호 물때와 맞으면 ×1.08, 어긋나면 ×0.78.
- **`starType`/`starCount` 부활.** [3차] Z 에 "UI 에서 안 쓰임 — 되살릴지 지울지 결정 필요"
  로 남아 있던 항목입니다. Solid/Open 의미를 부여해 컨디션 카드에 '스웰 품질'로 노출.
- **`swellClass`** 추가 (GROUND/MIXED/WIND). 풍파일 때 파장까지 계산해 설명합니다.

## I.3 검증 — 실측 대조

| 스팟 | 이전 | 현재 | 실측 체감 |
|---|---|---|---|
| 천진 | 80 | **3** | 0 (장판) |
| 봉수대 | 78 | **17** | 10 |
| 설악 | 72 | **14** | 20 |
| 물치 | 70 | **14** | 40 |

회귀 확인 (정상 조건이 눌리지 않는지):

```
0.6m/7s  오프쇼어  39 FAIR  ○Open      1.0m/8s  오프쇼어  67 GOOD  ○Open
1.2m/11s 오프쇼어  85 EPIC  ●Solid     1.5m/12s 무풍     100 EPIC  ●Solid
1.5m/6s  오프쇼어  45 FAIR  ○Open   ← 크지만 질 없음이 제대로 표현됨
1.0m/11s 온쇼어20  23 POOR  ○Open
```

## I.4 남은 갭과 대응 — `data/fieldReports.ts` (신규)

천진(장판)과 물치(탈 만함)는 **같은 9km 격자**라 모델 입력이 동일합니다.
차이를 만드는 건 곶·방파제 차폐, 근해 수심, 샌드바 — 격자가 볼 수 없는 것들입니다.
Surfline 은 이걸 bathymetry 로 물리 연산합니다(LOTUS). 우리는 그 데이터가 없습니다.

→ **현장 관측을 코드에 기록**하고, 같은 방향 관측이 2회 이상 쌓이면 그때
`swellWindow` 나 `LOCAL_BIAS` 를 조정합니다. 한 번으로 상수를 바꾸면 그날에 과적합됩니다.

천진에는 관측 1회 근거로 `swellWindow: [95, 175]` 를 넣었습니다(북동~동북동 차폐 의심).
**반증이 나오면 되돌리세요.**

---


# [9차] H. 조위를 조화분해로 연장 · 랭킹 거리 상한

## H.1 "조위는 전년도 평균이나 예측값을 넣을 수 없나"

넣을 수 있습니다. **조석은 기상이 아니라 천문 현상**입니다. 달·태양의 위치로
결정되므로 원리상 몇 년 뒤도 계산됩니다. Open-Meteo 가 조위를 해양 모델 응답에
얹어 주기 때문에 모델이 끝나는 9일에서 같이 끊길 뿐입니다.

`src/utils/tideHarmonics.ts` (신규) — 받아 온 구간에 조화 상수를 맞춰 연장합니다.

```
h(t) = Z₀ + Σ [ Aₖ·cos(ωₖt) + Bₖ·sin(ωₖt) ]
```

ωₖ(각속도)는 천문학적으로 고정이라 미지수는 Z₀ 와 Aₖ·Bₖ 뿐입니다. 분조 9개
(M2·S2·N2·K1·O1·P1·Q1·M4·MS4)로 미지수 19개, 샘플 200여 개 → 최소제곱으로 풉니다.
정규방정식을 직접 누적하고 부분 피벗팅 가우스 소거로 해를 구합니다.

## H.2 실측 검증 — 앞 2/3 학습 → 뒤 1/3 예측

| 스팟 | 조차 | 조위 MAE | 조차 대비 | **만조 시각 오차** |
|---|---|---|---|---|
| 태안 만리포 | 612cm | 34cm | 5.6% | **평균 0.50h** (최대 1h) |
| 제주 중문 | 235cm | 32cm | 13.5% | 평균 1.00h (최대 2h) |
| 부산 송정 | 104cm | 18cm | 16.8% | 평균 0.83h (최대 2h) |
| 양양 38선 | 44cm | — | — | **적합 거부** |

서핑에서 중요한 건 절대 수위가 아니라 **만조·간조 시각**이고, 그게 30분~1시간
오차로 맞습니다. "만조 전후 2~3시간" 판단에는 충분합니다.

### 🔑 스스로 거부하는 게 핵심입니다

동해는 조차가 40cm 남짓이라 조위의 상당 부분이 조석이 아닌 잔차(취송류·모델 잡음)
입니다. 여기에 억지로 조석을 맞추면 그럴듯한 가짜 곡선이 나옵니다.
그래서 **관측 구간 R² < 0.9 면 예측을 내놓지 않고** null 로 둡니다
(→ "조위 예보 범위 밖" 패널). 틀린 조위는 없는 것보다 나쁩니다.
동해는 애초에 `tidePreference: 'ANY'` 라 영향도 작습니다.

## H.3 출처를 얼굴로 구분합니다

`HourlyForecast.tidePredicted` / `DailyForecast.tidePredicted` 추가.

- 물때 차트: 곡선을 **점선**으로 + "조화분해 예측값" 안내(국립해양조사원 물때표
  참조 권고 포함). 색이 아니라 선 모양으로 구분합니다.
- 시간별 표: 예측값에 점선 밑줄 + 골드 잉크 + 툴팁

> 9일 샘플로는 주기가 비슷한 분조를 완전히 분리하지 못합니다(S2/K2 분리에 약 182일).
> **국립해양조사원 물때표를 대체하지 않습니다.**

## H.4 랭킹 거리 상한 — 284km 는 '인접'이 아닙니다

만리포(서해)를 고르면 랭킹 1~3위가 **284km 떨어진 동해 스팟**이었습니다.
점수순으로는 맞지만 당일치기로 갈 거리가 아닙니다. 스팟이 드문 권역에서
'가까운 8개 격자'가 반도를 가로질러 버린 탓입니다.

→ `NEARBY_MAX_KM = 150`. 후보가 4개 미만이면 제한을 풀어 가까운 순으로 채웁니다.
만리포 기준으로 이제 서해 5곳(5~76km)만 나옵니다.

---


# [8차] G. 🔴 10~16일차가 가짜였습니다 — 모델 병합 · 조위 한계 명시

## G.1 무엇이 잘못돼 있었나

"9월 12일 정확도가 얼마나 되냐, 만조 수위도 안 나온다" 는 질문에서 출발해
응답을 직접 까 봤습니다.

```
GET marine?...&forecast_days=16   (best_match, 기본값)
  2026-08-30 ~ 09-07   파고 null  0 / 24
  2026-09-08           파고 null 15 / 24
  2026-09-09 ~ 09-14   파고 null 24 / 24   ← 전부 null
```

**기본 모델은 9일에서 끊깁니다.** 그런데 `surfApi.ts` 가 `waveHeights[i] ?? 0.6`,
`seaLevels[i] ?? 0` 로 null 을 삼키고 있어서, **10~16일차가 "파고 0.6m · 조위 0cm"
인 멀쩡한 예보처럼 그려지고 있었습니다.** 물때 차트는 0cm 직선이라 종일 간조처럼
보였고요. 없는 것보다 나쁜 정보였습니다.

## G.2 모델별 실측

| 모델 | 파고 마지막 | 조위 마지막 |
|---|---|---|
| `best_match` (기본) | 09-08 | **09-08** |
| `ncep_gfswave025` | **09-14 (16일)** | 전부 null |
| `ncep_gfswave016` | 09-14 | 전부 null |
| `ecmwf_wam025` | 09-13 | 전부 null |
| `gwam` | 09-06 | 전부 null |
| `meteofrance_wave` | 09-08 | 전부 null |

→ **파고는 16일까지 얻을 수 있지만, 조위는 어떤 모델에서도 9일이 한계입니다.**

## G.3 처리

**파고·주기·스웰방향** — `models=best_match,ncep_gfswave025` 로 두 모델을 함께 받고
(필드에 접미사가 붙습니다) best_match 가 null 인 구간만 gfswave 로 메웁니다.
이제 10~16일차에 **실제 모델 값**이 들어갑니다.

**조위** — 없는 구간은 만들지 않습니다.
- `HourlyForecast.tideAvailable` / `DailyForecast.hasTide` 추가
- 물때 차트: "이 날짜는 조위 예보 범위 밖입니다" 안내 패널로 대체
- 시간별 표 물때 행: 값 대신 `–`

**신뢰도** — `DailyForecast.confidence` (D+0~2 HIGH / D+3~6 MEDIUM / D+7~ LOW).
스트립에서 LOW 인 날은 **테두리를 점선**으로 하고 `≈` 를 답니다. 색을 바꾸면
판정색(좋음/평범/나쁨)과 충돌하므로 색이 아닌 채널을 씁니다. 범례에 한 줄 추가.

## G.4 "어디로 갈까" 를 16일로

날짜 칩을 7일 → **16일**(예보 최대치)로 늘리고 가로 스크롤로 넘깁니다.

처음에 7일로 잡은 근거("8스팟 16일이면 수 MB")가 **틀렸습니다.** 실측:

```
8스팟 × 16일 × 다중모델   marine 178KB + weather 86KB = 264KB
```

랭킹도 같은 모델 병합을 씁니다. 파고가 null 인 시각은 배열에 아예 담지 않아
'플랫'인 척하지 않습니다.

## G.5 검증 (9/14 선택)

| 항목 | 결과 |
|---|---|
| 날짜 칩 | 16개 (오늘~9/14) |
| 9/14 랭킹 | 물치 100점 2.4m·11초 — **실제 모델 값** (이전엔 0.0m) |
| 스트립 점선 카드 | 8개 (D+7~) |
| 9/14 물때 패널 | "조위 예보 범위 밖" 안내로 대체 |

---


# [7차] F. Surfline 서버 조회 · 좌표 교정 · "어디로 갈까" 랭킹 행

## F.1 🔴 APK 만 보고 "스팟은 못 얻는다"고 끝냈던 것

[6차] E.1 에서 "Surfline 스팟 DB 는 서버에 있고 APK 에는 없다" 까지 알아내고
**거기서 멈췄습니다.** 엔드포인트를 찾아 놓고 조회를 안 했습니다.

```
$ strings classes*.dex | grep -oE 'kbyg/[a-z/]+'
kbyg/mapview          ← 바운딩 박스 안의 스팟 전부
kbyg/spots/nearby
kbyg/spots/batch
search/site
```

**`kbyg/mapview` 는 인증 없이 열립니다.** 한반도 bbox 로 조회하면 한국 스팟이
좌표까지 전부 나옵니다.

```
GET https://services.surfline.com/kbyg/mapview?south=32.9&west=124.4&north=38.9&east=129.9
→ data.spots[] : { name, lat, lon, _id }  — 한국 56곳
```

> ⚠️ 셸(curl)에서는 샌드박스가 가로채 403/502 가 옵니다. 브라우저 도구로
> `www.surfline.com` 오리진에서 `fetch` 하면 정상 응답합니다.

## F.2 좌표 30곳 교정 — 최대 7.6km 틀려 있었습니다

[5차]에서 제가 지식으로 찍은 좌표를 Surfline 실좌표와 대조했습니다.

| 스팟 | 오차 |
|---|---|
| 동호 | **7.6km** |
| 정암 | **6.4km** |
| 동산 | 4.1km |
| 남열 · 봉수대 | 3.7km |
| 설악 | 3.2km |
| 자작도 | 2.9km |
| 맹방 · 대진 | 2.0~2.2km |

해양 격자가 9km 라 예보값은 대개 같은 셀에 떨어졌지만, **지도 마커가 엉뚱한 데
찍히고** 있었습니다. 62곳 전부 `verify:spots` 재통과.

**스팟 좌표는 앞으로 Surfline `kbyg/mapview` 로 교차 검증합니다.**

## F.3 새 최상단 행 — "어디로 갈까" (`NearbyRanking.tsx`)

16일 스트립은 *"이 스팟이 언제 좋은가"* 를 답합니다. 주말 아침에 먼저 나오는
질문은 하나 더 있습니다: **"그래서 어디로 가지?"**

- 선택 스팟 주변 **8개 지역**을 고른 날짜·시간대 기준 **점수순**으로 정렬
- 날짜 칩 7일 · 시간대 칩(종일/아침/낮/오후 — 스트립의 DAY_PARTS 와 같은 정의)
- 카드: 순위 · 스팟명 · 판정+점수 · **베스트 시간대** · 파고/주기 · 바람 다이얼 · 거리
- 카드를 누르면 그 스팟으로 전환

### 🔑 격자 한 칸당 한 곳만 세웁니다

그냥 '가까운 8곳'을 뽑았더니 **카드 8개가 전부 70점·0.9m 로 똑같이** 나왔습니다.
양양은 38선·하조대·죽도·인구·동호가 **같은 9km 격자 한 칸**에 들어가서, 파고·주기가
물리적으로 동일하기 때문입니다. 랭킹이 그냥 거리순 목록이 됩니다.

→ 격자 셀당 가장 가까운 한 곳만 대표로 세우고, 같은 칸에 묶인 나머지는
`+4곳` 으로 표시합니다. 선택한 스팟은 대표 자리를 양보하지 않습니다.

부산권에서 검증: 흥환 76 · 나정 73 · 진하 62 · 송정 59 · 구조라 51 · 임랑 49 ·
다대포 48 · 흥남 6 — **8곳 전부 다른 점수**.

### 수집은 한 번, 재정렬은 로컬

`fetchNearbySeries()` 가 7일치 시계열을 **요청 2개**로 받아 두고(Open-Meteo 는
위경도를 콤마로 이으면 다중 좌표를 한 번에 줍니다), 날짜·시간대를 바꾸면
`rankNearby()` 가 받아 둔 배열을 다시 집계만 합니다. 옵션을 누를 때마다 로딩이
걸리면 비교 자체가 안 되기 때문입니다.

16일이 아니라 **7일**인 이유: 8스팟 × 16일이면 응답이 수 MB 라 모바일에 부담이고,
이 기능이 답하는 질문("이번 주말 어디로")에는 7일이면 항상 다음 주말이 들어옵니다.

선택 스팟 예보와는 **별도 이펙트**입니다. 묶으면 8곳 수집이 끝날 때까지 16일
스트립까지 같이 기다리게 됩니다.

---


# [6차] E. 스팟 41 → 62개 · 지도 톤 복원 · 스팟 전환 시 지도 유지

## E.1 🔴 스팟 목록의 출처가 잘못돼 있었습니다

[5차] A 에서 스팟 41개를 넣을 때, **출처가 제 지식이었습니다.** Surfline APK 를
참고했다고 적었지만 실제로 APK 에서 나온 건 스팟 가이드의 *항목 구조*(Ability
Level · Bottom · Best Season · Hazards · Crowd)뿐이고 스팟 자체는 아니었습니다.

```
$ grep -ac "Seorak\|Jukdo\|Mallipo\|Jungmun" classes*.dex   →  0
$ strings classes*.dex | grep surfline.com                    →  services.surfline.com
```

**Surfline 의 스팟 DB 는 서버(`services.surfline.com`)에 있고 APK 에는 없습니다.**
APK 벤치마킹으로는 애초에 스팟 목록을 얻을 수 없습니다. 그 결과 설악해변 같은
주요 스팟이 통째로 빠졌습니다.

## E.2 🔑 진짜 출처 — 사용자가 만들어 둔 NotebookLM 노트북

사용자가 별도로 조사해 **Gemini 서브에이전트로 저장해 둔 지식**이 있었는데,
`~/.claude/agents` 만 뒤지느라 놓쳤습니다. 실제 위치:

```
~/.gemini/config/agents/surf_guide/AGENT.md
  └─ NotebookLM 노트북 6f3f58fd-4ae7-4a9b-a216-0d29f8c5b8f7
     "The Essential Guide to Surf Forecast Sites and Tools"
```

조회는 `nlm` CLI (`~/.local/bin/nlm`, 인증 완료 상태):

```bash
nlm notebook query 6f3f58fd-4ae7-4a9b-a216-0d29f8c5b8f7 "질문"
```

여기에 **전국 30여 스팟의 권역·행정구역·바닥·실력대·파도특성 매트릭스**가
이미 정리돼 있었습니다. 스팟 관련 작업은 앞으로 이 노트북을 1차 출처로 씁니다.

## E.3 추가된 21곳 → 총 62곳

| 권역 | 계 | 추가 |
|---|---|---|
| 동해 | 33 | **설악** · 물치 · 정암(리버마우스) · 동호 · 동산 · 지경 · 송지호 · 백도 · 봉수대 · 천진 · 속초 · 대진 · 용화 |
| 남해 | 8 | 임랑 · **고흥 남열**(전남 유일) |
| 제주 | 15 | 삼양 · 협재 · 금능 · 월령 · 신양섭지 · 화순금모래 |
| 서해 | 6 | — |

노트북이 짚은 스팟별 특성을 설명·실력대에 반영했습니다. 예: **설악**은 수심 편차가
커서 경사면에서 순식간에 말리는 **덤퍼**라 상급, **정암**은 하천이 만든 리버마우스
A프레임이라 상급.

`npm run verify:spots` — **62곳 전부 통과** (marine API 응답 · 방위 규약 · 근접 중복).

### 넣지 않은 것

- **시흥 웨이브파크** — 인공 풀이라 해양 예보가 의미 없습니다.
- **포항 신항만** — 노트북에 있지만 좌표를 특정할 근거가 부족해 보류했습니다.
  (지어내면 지도에 엉뚱한 곳이 찍힙니다)

## E.4 지도 톤 복원

OSM 표준 타일을 그대로 쓰니 채도가 높아(초록 산림·분홍 도로) 지도가 혼자 튀고
마커 판정색이 배경에 묻혔습니다. 예전 CARTO Positron 계열의 옅은 캔버스로 되돌렸습니다.

```css
.leaflet-tile { filter: saturate(0.32) brightness(1.06) contrast(0.9); }
:root[data-theme='night-swell'] .leaflet-tile {
  filter: invert(1) hue-rotate(180deg) saturate(0.42) brightness(0.88) contrast(0.88);
}
```

## E.5 🔑 스팟을 바꿔도 지도가 사라지지 않습니다

`App.tsx` 의 `isLoading` 분기가 **지도까지 감싸고 있었습니다.**

```jsx
{isLoading ? <Skeleton /> : <> ...<SpotMapView />... </>}   // ← 예전
```

지도에서 스팟을 찍는 순간 지도 자체가 언마운트 → 재마운트돼 줌·중심이 초기화되고
화면이 통째로 깜빡였습니다. **지도는 스팟을 고르는 컨트롤 자체**라 조작 중에
사라지면 안 됩니다.

→ `<SpotMapView>` 를 로딩 분기 **밖**으로 빼고, 예보 패널만 스켈레톤으로 바꿉니다.
스켈레톤 높이를 실제 패널과 맞춰(190/86, 420/300) 지도가 위아래로 튀지 않게 했습니다.
재요청 중임은 지도 헤더의 배지로만 알립니다.

**검증:** 스팟 칩 클릭 전후로 `.leaflet-container` 가 **동일한 DOM 노드**이고
`_leaflet_id` 도 같습니다 — 언마운트되지 않습니다.

---


# [5차] D. 🔴 배포 경로 정정 — 이 프로젝트는 git push 로 배포되지 않습니다

## D.1 무엇이 틀려 있었나

[4차] U 와 전역 규칙은 "git push 하면 Cloudflare 가 자동 빌드" 를 전제로 쓰였습니다.
**이 프로젝트에는 해당되지 않습니다.**

```
$ npx wrangler pages project list
│ ypod   │ ypod.pages.dev, ypod.forges.work   │ Yes │   ← git 연동됨
│ padoh  │ padoh.pages.dev, wwf.forges.work   │ No  │   ← 연동 안 됨
```

`padoh` 은 GitHub 연동이 없습니다. 지금까지의 배포는 전부 **직접 업로드**였습니다.
push 후 6분을 기다려도 반영이 안 돼서 찾았습니다.

## D.2 실제 배포 방법

```bash
npm run build
npx wrangler pages deploy dist --project-name padoh --branch main --commit-dirty=true
```

업로드 후 **약 10초**면 canonical URL 에 반영됩니다.
(전역 규칙의 "로컬 deploy 를 쓰지 말 것" 은 Vercel 얘기입니다. Cloudflare 는 잘 됩니다.)

## D.3 ⚠️ wrangler 출력을 믿지 마세요

`wrangler pages deployment list` 의 **Source 컬럼이 커밋을 잘못 표기합니다.**
"0e0b8d5" 로 찍힌 배포본이 실제로는 그 이후 커밋(`f5d98fa`)의 WWF 브랜딩을
서빙하고 있었습니다. 직접 업로드 시점의 로컬 git 컨텍스트를 찍는 것으로 보입니다.

**검증은 언제나 canonical URL 의 실제 내용으로 합니다** — 빌드 자산 해시를
`dist/index.html` 과 대조하는 게 가장 확실합니다.

## D.4 커밋 작성자 이메일 (GH007)

전역 `~/.gitconfig` 은 `owennjerry@gmail.com` 인데, GitHub 계정 `forge-owen` 은
"Block command line pushes that expose my email" 이 켜져 있어서 등록된 실제 메일
(`obvs.work@gmail.com`)로도 `GH007` 로 거부됩니다. 레포 로컬로 아래를 잡아 뒀습니다.

```
user.email = 295408396+forge-owen@users.noreply.github.com
```

전역 설정은 건드리지 않았습니다. GitHub 설정에서 보호를 끄면 실제 메일로 되돌릴 수 있습니다.

## D.5 배포 확인 (2026-08-30)

| 항목 | 결과 |
|---|---|
| `wwf.forges.work` · `padoh.pages.dev` | ✅ HTTP 200 |
| 라이브 자산 해시 | `index-BxR22zP4.css` / `index-CClTrYt6.js` — 로컬 빌드와 일치 |
| 스팟 셀렉터 · 지도 마커 | ✅ 41개 / 41개 |
| 권역 탭 | 전국 41 · 동해 20 · 남해 6 · 제주 9 · 서해 6 |
| 지도 타일 | `tile.openstreetmap.org` (워터마크 없음) |
| 문서 가로 스크롤 | ✅ 없음 |

> [4차] U.3 의 CNAME 미해결 항목은 그사이 해결돼 있었습니다 — `wwf.forges.work` 정상 응답.

---


# [5차] A. 스팟 16 → 41개 · 스팟 가이드 · 모바일 가로축 고정 (2026-08-30)

> ⚠️ **병렬 작업 주의** — 이 라운드는 `src/index.css` · `src/components/*` 를 광범위하게
> 건드렸습니다. 다른 세션에서 같은 파일을 잡고 있었다면 먼저 diff 를 맞춰 주세요.
> 시작 시점의 워킹트리에 이미 들어와 있던 변경(스팟 9개 추가 + 스웰 방위/물때/
> 크로스스웰 점수 보정)은 **그대로 살려서 그 위에 얹었습니다.**

## A.1 스팟 41개 — `src/data/koreaSurfSpots.ts` (신규)

스팟 목록을 `surfApi.ts` 밖으로 뺐습니다. 수집 로직 파일 안에 데이터 800줄이 앉아
있으면 둘 다 읽기 어려워집니다. `surfApi.ts` 가 그대로 re-export 하므로
**기존 import 경로(`from '../services/surfApi'`)는 하나도 안 깨졌습니다.**

| 권역 | 개수 | 새로 들어온 곳 |
|---|---|---|
| 동해 | 20 | 아야진 · 외옹치 · 낙산 · 하조대 · 남애 · 사천진 · 경포 · 망상 · 맹방 · 후정 · 칠포 · 나정 · 진하 |
| 남해 | 6 | 해운대 · 다대포 · 구조라 |
| 제주 | 9 | 이호테우 · 함덕 · 김녕 · 하도 · 표선 |
| 서해 | 6 | 을왕리 · 신두리 · 꽃지 · 대천 |

**방위 규약을 문서화하고 강제했습니다.**
`optimalSwellDeg` = 해변이 바라보는 방위 = 스웰이 들어오는 방위,
`optimalWindDeg` = 그 반대편(오프쇼어). 두 값이 180°에서 크게 어긋나면 WindDial
화살표와 스웰 굴절 보정이 **동시에** 틀어지는데, 이걸 눈으로 잡을 방법이 없었습니다.

## A.2 🔑 `npm run verify:spots` — 스팟은 이제 실측 검증합니다

`scripts/verify-spots.mjs` (신규). 좌표를 하나 잘못 찍으면 앱에서는 그냥
"예보를 못 불러왔습니다" 나 조용한 0 으로만 보입니다 — **Open-Meteo marine API 는
육지 격자에서 파고를 null 로 돌려주기 때문입니다.** 세 가지를 실제로 확인합니다:

1. 방위 규약 (180° ± 35°)
2. id 중복 · 좌표 400m 이내 근접 (지도 마커가 겹칩니다)
3. marine API 가 그 좌표에서 실제로 파고를 돌려주는지 — **41개 전부 통과 확인**

### ℹ️ 알아 둘 것 — 해양 격자는 약 9km 라 이웃 스팟이 격자를 공유합니다

```
· 고성 아야진 / 속초 외옹치
· 양양 38선 / 하조대 / 죽도 / 인구
· 양양 남애 / 강릉 사천진 / 강릉 경포
· 포항 칠포 / 포항 월포
· 제주 김녕 / 제주 월정리
```

이 스팟들은 **파고·주기가 똑같이 나옵니다.** 버그가 아니라 모델 해상도의 한계입니다.
점수는 스팟별 오프쇼어 방위·스웰 굴절·물때로 여전히 갈립니다. 검증 스크립트가
매번 이 목록을 출력하니 "왜 두 해변이 같지?" 를 다시 조사하지 마세요.

## A.3 스팟 가이드 — Surfline 이 담는 축을 한국식으로

Surfline 스팟 가이드(Ability Level · Bottom · Best Season · Hazards · Crowd Factor)를
참고해 `SurfSpot` 에 5개 축을 넣었습니다. **예보 수치는 "오늘 파도가 어떤가"만
답하지, "거기 나한테 맞는 데야?" 는 답하지 못합니다.**

`skillLevel` · `bestSeasons` · `tidePreference` · `crowdLevel` · `hazards[]`
→ 라벨·색은 전부 `scoreVisuals.ts` 에 모았고(표현 규칙 단일 소스 원칙),
가이드 모달의 '스팟 특징' 카드에 2열로 붙습니다. 위험 요소는 권역 공통 주의사항
아래에 스팟별 칩으로 따로 답니다.

`SpotHeader` 의 로컬 `BOTTOM_LABEL` 도 공용 `BOTTOM_META` 로 흡수했습니다.

## A.4 🔴 지도 타일 — CARTO 가 워터마크를 찍고 있었습니다

**프로덕션에서 지도 위에 "API KEY REQUIRED" 글자가 깔리고 있었습니다.**
CARTO 가 키 없는 basemap 요청에 워터마크를 찍도록 정책을 바꿨는데,
**타일 응답이 200 이라 콘솔·네트워크 로그에는 아무 흔적이 없습니다.** 눈으로만 보입니다.

→ OSM 표준 타일(`tile.openstreetmap.org`)로 교체. README 의 "API 키가 필요 없습니다"
약속을 지켰습니다. OSM 은 라이트 한 종류뿐이라 **다크 테마는 타일을 새로 받지 않고
CSS 필터로 반전**시킵니다(`invert(1) hue-rotate(180deg)` — 바다 파랑이 주황으로
뒤집히는 걸 hue-rotate 가 되돌립니다). 타일 요청량도 절반이 됐습니다.

## A.5 지도 마커 디클러터

41개가 되니 전국 뷰에서 이름표가 서로 겹쳐 아무것도 못 읽게 됐습니다.
`LABEL_ZOOM = 8.5` 미만에서는 **점만** 찍고, 확대하면 이름표를 붙입니다.
선택된 스팟만 축척과 무관하게 항상 이름표를 답니다.

## A.6 헤더 스팟 셀렉터를 모바일에도 노출

셀렉터가 `hidden md:flex` 였습니다. 스팟이 7개일 때는 지도 마커로 골라도 됐지만
41개를 모바일 지도에서 찍어 고르는 건 불가능합니다. 전 화면폭에 노출하고,
자리는 브랜드 부제를 `hidden sm:block` 으로 접어 마련했습니다.

---

# [5차] B. 🔧 모바일 가로축 고정 — 전 프로젝트 표준으로 승격

## B.1 증상과 진짜 원인

"모바일에서 좌우가 안 잡히고 밀린다"는 **항상 두 가지 중 하나**입니다.

1. **뷰포트보다 넓은 자식**이 문서 전체에 가로 스크롤을 만든다
   (이 앱에서는 `min-w-[900px]` 시간별 표)
2. **가로 스크롤 영역의 스와이프가 끝에 닿는 순간 페이지로 전파**된다 (scroll chaining)
   → 16일 스트립·시간별 표·권역 탭·스팟 칩, 네 군데 전부 해당

하나만 막으면 증상이 남습니다. 그래서 매번 "고쳤는데 또 그러네" 가 됩니다.

## B.2 ⚠️ `overflow-x: hidden` 이 아니라 `clip` 인 이유

이게 이 문제의 핵심이고, 가장 많이 틀리는 지점입니다.

> `hidden` 은 **스크롤 컨테이너(scrollport)를 만들어서 그 안의 `position: sticky`
> 를 죽입니다.** 이 앱은 헤더가 sticky 라, hidden 을 쓰면 가로 밀림은 잡히는 대신
> **헤더가 아예 안 붙습니다.** `clip` 은 스크롤 컨테이너를 만들지 않고 넘친 부분만
> 잘라내므로 sticky 가 그대로 삽니다.

```css
html { overflow-x: clip; }
body { overflow-x: clip; overscroll-behavior-x: none; }
```

## B.3 `.scroll-x` — 가로 스크롤 영역의 표준형

`overflow-x-auto no-scrollbar` 조합을 전부 이걸로 바꿨습니다(4곳).

```css
.scroll-x {
  overflow-x: auto;
  overscroll-behavior-x: contain;   /* ← 전파를 여기서 끊습니다 */
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none; -ms-overflow-style: none;
}
```

`overflow-y` 는 일부러 안 건드립니다. 같이 잠그면 스트립 카드 위로 삐져나온
'베스트' 배지가 잘립니다.

## B.4 실측 검증 (375×812)

눈으로 보지 않고 측정했습니다.

| 항목 | 결과 |
|---|---|
| `documentElement.scrollWidth` vs `clientWidth` | 375 = 375 (가로 스크롤 없음) |
| 조상이 안 가둔 채 뷰포트를 넘는 요소 | **0개** |
| 스트립을 끝(1423px)까지 민 뒤 `document.scrollLeft` | **0 유지** (전파 차단됨) |
| sticky 헤더 | `left: 0`, `width: 375` 고정 |

## B.5 전역 규칙으로 올렸습니다

`~/.claude/CLAUDE.md` 에 **"모바일 웹 — 가로축 고정 (전 프로젝트 기본값)"** 섹션을
추가했습니다. CSS 블록 · clip vs hidden 경고 · 하지 말 것 목록 · 콘솔 검증 스니펫이
들어 있어, 앞으로 새 웹 프로젝트는 시작할 때 이걸 먼저 깔면 됩니다.
`viewport-fit=cover` + `env(safe-area-inset-*)` 도 같이 표준화했습니다
(`user-scalable=no` 는 넣지 않습니다 — 저시력 사용자가 표를 못 읽습니다).

---


# [5차] C. 모바일 하드닝 2차 — iOS 입력창 확대 · dvh · hover · 터치 타겟

B 에서 가로축만 잡았는데, 모바일 고질병이 그거 하나가 아니었습니다.
전부 `src/index.css` 한 곳에서 끝나고, 전역 규칙(`~/.claude/CLAUDE.md`)과 동일합니다.

## C.1 🔴 iOS Safari 입력창 확대 — 함정이 하나 더 있었습니다

**원인은 확대 설정이 아니라 글자 크기입니다.** iOS Safari 는 포커스된 폼 컨트롤의
계산된 `font-size` 가 16px 미만이면 자동으로 줌인하고, 줌아웃은 안 해 줍니다.
헤더 스팟 셀렉터가 `text-sm`(14px) 이라 정확히 걸려 있었습니다.

```css
@media (pointer: coarse) { input, select, textarea, [contenteditable] { font-size: 16px; } }
```

**→ 이걸 넣었는데도 안 먹었습니다.** 검증 스니펫 ③번이 `SELECT: 14px` 로 잡아냈습니다.

### 왜 안 먹었나 — Tailwind 쓰면 반드시 걸리는 함정

```
.text-sm  (0,1,0)   >   select  (0,0,1)      ← 유틸리티가 이긴다
```

Tailwind v3 의 `@layer base/components/utilities` 는 **네이티브 CSS 캐스케이드
레이어가 아니라 빌드 때 사라지는 지시자**입니다. 그래서 "레이어 밖으로 빼면 이긴다"
가 통하지 않고 (처음에 그렇게 시도했다가 실패), 평범한 특이성 싸움이 됩니다.
파일 뒤쪽에 써도 소용없습니다 — 소스 순서는 특이성이 같을 때만 의미가 있습니다.

**해법:** `[class]` 를 붙여 (0,1,1) 로 올립니다. 클래스 없는 순수 `<input>` 을 위해
맨 선택자도 같이 둡니다.

```css
input[class], select[class], textarea[class], [contenteditable][class] { font-size: 16px; }
```

`!important` 는 쓰지 않았습니다 — 의도적으로 20px 로 키운 입력창까지 16px 로
**깎아내립니다.** 필요한 건 "최소 16px" 이지 "무조건 16px" 이 아닙니다.

`(pointer: coarse)` 로 감쌌으므로 **데스크톱은 14px 그대로**입니다.

> 🚫 `user-scalable=no` / `maximum-scale=1` 로 막지 않았습니다.
> iOS 10 부터 Safari 가 무시하고, 먹는 브라우저에서는 저시력 사용자가 확대를
> 못 하게 됩니다. 해법은 언제나 16px 쪽입니다.

## C.2 `100vh` → `dvh`

iOS 의 `100vh` 는 **URL 바가 펼쳐진 상태 높이**라 실제 보이는 영역보다 큽니다.
푸터가 잘리고 스크롤 중 높이가 튑니다. `min-h-screen` 두 곳을 `.min-h-dvh-safe`
(vh 폴백 → dvh 덮어쓰기)로 교체했습니다.

## C.3 터치에서 `:hover` 들러붙음

터치 기기에서 `:hover` 는 탭한 뒤 **다른 곳을 탭할 때까지 남습니다.**
`.card-interactive` · `.segmented` · `.btn-primary` · `.btn-ghost` · Leaflet 줌 버튼의
호버 규칙을 전부 `@media (hover: hover) and (pointer: fine)` 안으로 옮기고,
터치용 피드백으로 `.btn:active { transform: scale(0.97) }` 를 새로 줬습니다.

## C.4 터치 타겟 — `.tap-safe`

권역 필터 탭과 지도 스팟 칩이 **높이 28px** 였습니다(HIG 최소 44px).
보이는 크기를 키우면 밀도가 무너지므로, 가상 요소로 **히트 영역만** 넓힙니다.

```css
.tap-safe::after { content:''; position:absolute; top:50%; left:50%;
  width:max(100%,44px); height:max(100%,44px); transform:translate(-50%,-50%); }
```

`elementFromPoint` 로 검증: 버튼 시각 경계 **위·아래 6px 지점이 버튼에 들어가고**,
인접 탭을 침범하지 않습니다.

## C.5 기타

- `overflow-wrap: break-word` (body) — 긴 URL·해시가 가로로 미는 것 차단
- `touch-action: manipulation` — 더블탭 확대 대기(약 300ms) 제거
- 셀렉터가 16px 로 커진 만큼 `max-w-[44vw]` → `52vw`

## C.6 검증 (375×812, `pointer: coarse` 에뮬레이션)

| 항목 | 결과 |
|---|---|
| ① 문서 가로 스크롤 | ✅ 없음 (375 = 375) |
| ② 조상이 안 가둔 채 뷰포트를 넘는 요소 | ✅ 0개 |
| ③ 16px 미만 폼 컨트롤 | ✅ 없음 (SELECT 14 → 16px) |
| ④ `.tap-safe` 히트 영역 | ✅ 시각 경계 밖 6px 도 버튼에 적중 · 인접 탭 침범 없음 |

검증 스니펫은 `~/.claude/CLAUDE.md` §8 에 그대로 있습니다.
**소스만 보고 넘어가면 C.1 함정을 그대로 통과합니다 — 반드시 계산값을 측정하세요.**

---


# [3차] X. 바람 표시를 전부 갈아엎었습니다 — `WindDial`

## X.1 문제 진단

"화살표 방향이랑 색상이 뭘 뜻하는지 모르겠다"는 피드백은 **UI 문제가 아니라 개념 문제**였습니다.

**오프쇼어/온쇼어는 절대 방위가 아닙니다.** 해변이 어느 쪽을 바라보느냐에 따라 완전히 달라집니다:

| 스팟 | 해변이 보는 쪽 | 오프쇼어 바람 |
|---|---|---|
| 양양 38선 | 동(90°) | **서풍** 270° |
| 태안 만리포 | 서(280°) | **동풍** 100° |
| 제주 중문 | 남남서(200°) | **북풍** 20° |

그러니까 나침반 화살표나 "SSE" 같은 방위 표기를 아무리 예쁘게 그려도, 사용자는 그게 좋은
바람인지 알 수 없습니다. **Surfline 이 헷갈리는 이유가 정확히 이것입니다.**
(데이터에는 스팟별 `optimalWindDeg` 가 이미 다 다르게 들어 있었는데, UI 가 그걸 안 쓰고
절대 방위만 보여주고 있었습니다.)

## X.2 해결 — 해변 단면 다이어그램

`src/components/WindDial.tsx` 로 나침반을 대체했습니다. 스팟이 바뀌어도 **그림의 의미가
고정**됩니다:

```
   ┌─────────┐
   │ 🏖 해변  │   ← 위쪽은 언제나 육지
   ├────↓────┤   ← 화살표 = 바람이 흘러가는 방향
   │ 🌊 바다  │   ← 아래쪽은 언제나 바다 (물결무늬로 한 번 더 표시)
   └─────────┘
```

- **화살표가 아래(바다)로** → 육지에서 바다로 부는 바람 → **오프쇼어** → 좋음 (초록)
- **화살표가 위(해변)로** → 바다에서 육지로 → **온쇼어** → 나쁨 (빨강)
- **가로 두 줄** → 글래시 (바람이 거의 없어 방향이 의미 없음)

방위를 몰라도 그림만 보면 읽힙니다. 색은 보조일 뿐이고, 방향(도형)이 1차 채널입니다.

**회전 계산** (`shoreRelativeArrowRotation`): 기상 풍향에서 그 스팟의 오프쇼어 방위를 빼서
"오프쇼어에서 얼마나 어긋났는지"를 구하고, 완벽한 오프쇼어가 아래(180°)를 향하도록 뒤집습니다.
스팟마다 기준이 자동으로 달라집니다.

## X.3 어디에 적용됐나

| 위치 | |
|---|---|
| 16일 스트립 | 아침/낮/오후 각 칸에 24px 다이얼 |
| 컨디션 카드 | 46px 다이얼 + "육지에서 바다로 부는 바람. 파도 면을 세워 줘 가장 좋습니다." + "풍향 WSW · 이 해변의 오프쇼어는 270° 쪽" |
| 시간별 표 | '바람 방향' 행 전체 (기존 `OFFSHORE`/`S-ON` 텍스트 배지를 대체) |
| 범례 | 스트립 헤더에 "🡇 바다로 = 좋음 / 🡅 해변으로 = 나쁨" 항상 노출 |
| 스팟 가이드 모달 | **"오프쇼어가 뭔가요?"** 블록 신설 — 개념 + 해변마다 다른 이유를 설명 |

다이얼의 육지/바다 반쪽은 `--gold-soft`/`--tide-soft` 로는 24px 에서 구분이 안 돼서,
표면과 26~34% 섞은 값으로 직접 진하기를 잡았습니다. 화살표는 표면색 외곽선을 한 겹 깔아
어느 반쪽 위에서도 떠 보이게 했습니다.

---

# [3차] Y. 7일 → 16일 스트립, 최상단으로

`WeekStrip.tsx` → **`ForecastStrip.tsx`** (신규). `Forecast16DaysTable.tsx` 는 역할이
완전히 겹쳐서 삭제했습니다.

## Y.1 무엇이 바뀌었나

- **7일 → 16일.** API 도 `forecast_days=14` → `16` (양쪽 다 384시간 응답 확인).
  가로 스와이프 + 좌우 스크롤 버튼으로 넘깁니다.
- **화면 최상단**으로 올렸습니다. 이제 이게 메인 컨트롤입니다.
- **헤더를 한 줄로 압축**: `[📍스팟명] │ [브리핑 한 줄] ......... [범례]`
- 세로 여백을 전반적으로 줄여(패널 패딩·간격·지도 높이) 첫 화면에 더 많이 들어갑니다.

## Y.2 🔑 날짜 선택이 하위 전체를 구동합니다

이번 작업의 핵심 구조 변경입니다. `App.tsx` 에 `selectedDateISO` 상태가 생겼고,
**스트립에서 날짜를 고르면 아래가 전부 그 날짜로 바뀝니다:**

```
스트립에서 날짜 클릭
   → 컨디션 카드   (그날 낮 시간대 중 최고 시각 기준)
   → 시간별 예보 표 (그날 24시간)
   → 물때 차트     (그날 조위)

지도/헤더에서 스팟 변경
   → 예보 재요청 + 날짜 선택을 오늘로 리셋 + 스트립 스크롤 맨 앞으로
```

상태 흐름은 이 두 갈래뿐입니다. 시간별 표에 있던 '오늘/내일' 탭은 스트립과 역할이
중복돼 제거했습니다.

## Y.3 브리핑 한 줄 — ⚠️ "AI" 가 아닙니다

`buildBriefing()` (`surfApi.ts`)는 **규칙 기반**입니다. LLM 호출이 아닙니다.
16일 예보에서 (1) 오늘 상태와 (2) 앞으로 가장 좋은 날을 뽑아 한 문장으로 잇습니다.

> "오늘은 파도가 약합니다. 내일 오후가 0.3–0.5m 로 앞으로 16일 중 가장 나은 편입니다."

UI 라벨에 "AI" 라고 쓰지 않았습니다 — 실제로 모델을 부르지 않는데 그렇게 표기하면
사용자를 오해시킵니다. 진짜 LLM 요약을 붙일 거라면 **이 함수 하나만 교체**하면 되고,
그때 라벨을 바꾸면 됩니다. (스팟명 + 파고/바람/물때/스코어를 프롬프트로 넘기면 됩니다.)

## Y.4 하루 판정을 낮 시간 기준으로

24시간 전체에서 최고점을 뽑으면 **새벽 3시가 피크로 잡히는 일이 흔합니다**(밤에 바람이
잦아들어 글래시가 되므로). 물리적으로는 맞지만 서핑 예보로는 쓸모가 없습니다.
→ `SURFABLE_HOURS = 05~20시` 를 두고, 하루 판정·피크 시각·대표 시각을 전부 이 범위에서만
고릅니다 (`pickBestSurfableHour`). 실제로 "00:00 전후" 로 뜨던 피크가 "05:00" 로 바뀌었습니다.

---

# [3차] Z. 기타

- `DailyForecast14Days` → **`DailyForecast`** 로 이름 정리 (1차 §5.3 에서 지적했던 14/16 혼용
  해소). `fetchLive16DaysForecasts` 의 반환 필드명 `daily16Days` 는 그대로입니다.
- `WeekendSurferSummary` 타입과 `calculateWeekendSummary()` 삭제 — 주말 판정은 스트립의
  주말 강조 + 브리핑이 대체합니다.
- `starColorVar()` 삭제 (미사용).
- **남은 것:** `HourlyForecast.starType` / `starCount` 는 여전히 타입·엔진에 있지만 UI 에서
  쓰이지 않습니다. 별점 체계를 되살릴지 지울지 결정이 필요합니다.

---

---

---

---

# [4차] U. 도메인 · 브랜딩 · 문의 메일 (2026-07-28)

> ⚠️ **병렬 작업 주의** — 이 항목은 Cloudflare 계정 설정을 건드립니다.
> 다른 세션에서 DNS/도메인을 만지고 있다면 아래 상태를 먼저 확인하세요.

## U.1 이름

**파도(padoh) → WWF — Weekend Wave Finder** 로 개명.
헤더 · `<title>` · OG 메타 · README 반영.

- 저작권: **© 2026 forge**
- 문의: **wwf@forges.work** (푸터에 mailto 링크)

## U.2 Cloudflare 현재 상태

| 항목 | 상태 |
|---|---|
| Zone `forges.work` | active · account `OBVS.WORK` (`9e9dc8ef…`) — Pages 계정과 **동일** |
| Pages 프로젝트 | `padoh` (내부 이름. Cloudflare 는 프로젝트 개명 불가) |
| 커스텀 도메인 `wwf.forges.work` | 등록됨 · **status: pending** ← DNS 대기 |
| Email Routing | enabled · synced · MX/SPF 정상 |
| 목적지 `obvs.work@gmail.com` | **인증 완료** |
| 규칙 `wwf@forges.work` → `obvs.work@gmail.com` | **ON** (수신 즉시 동작) |

## U.3 🔴 남은 한 가지 — CNAME 레코드

`wwf.forges.work` 가 아직 뜨지 않는 이유는 **DNS CNAME 레코드가 없어서**입니다.

wrangler 의 OAuth 토큰에는 `zone (read)` 만 있고 **`dns_records` 쓰기 권한이 없습니다.**
(`/zones/{id}/dns_records` POST → `code 10000 Authentication error`)
Pages 커스텀 도메인 API 는 이 플로우에서 DNS 를 자동 생성해 주지 않습니다.

**필요한 레코드 (딱 1개):**

```
Type    : CNAME
Name    : wwf
Target  : padoh.pages.dev
Proxy   : Proxied (주황 구름 ON)
TTL     : Auto
```

추가하면 Pages 가 자동으로 검증 → SSL 발급까지 진행합니다(보통 1~3분).

## U.4 이름 불일치 — 의도적으로 두었습니다

앱 이름은 WWF 인데 **GitHub 레포와 Pages 프로젝트는 `padoh`** 로 남아 있습니다.

- Pages 프로젝트는 Cloudflare 가 개명을 지원하지 않습니다(삭제 후 재생성만 가능).
  재생성하면 `padoh.pages.dev` 주소와 배포 이력이 사라집니다.
- GitHub 레포 개명은 가능하지만, **병렬 작업 경고가 있어 임의로 바꾸지 않았습니다.**
  다른 세션의 git remote 가 깨질 수 있습니다.

커스텀 도메인이 붙으면 사용자에게 보이는 주소는 `wwf.forges.work` 뿐이라 실사용에는 영향이
없습니다. 정리하고 싶다면 다른 세션 작업이 끝난 뒤 결정하세요.

---

# [3차-c] V. 컨디션 카드를 두 줄로 · 스트립 바로 아래로

## V.1 무엇이 문제였나

컨디션 카드가 지도 옆 우측 컬럼을 세로로 가득 채우는 큰 블록이었습니다:
히어로 숫자 40px + 미터 바 + 피크 타임 블록 + 3단 지표 리스트 + 스팟 설명문.
**약 420px.** 정보량 대비 세로 공간을 너무 많이 먹어서, 정작 "지금 어떤지"를 보려면
스크롤해야 했습니다.

## V.2 지금 구조 — 2행, 약 110px

```
1행) 📍스팟명  [모래] [●실시간]  ·········  [🎥] [ⓘ가이드]
2행) [평범 53] │ 파고 0.4m·6초 │ 스웰 5kJ │ ◐글래시 6km/h │ ☀️26° │ 지금 00:00 · 피크 06:00
```

- 2행은 칩이 가로로 흐르다 좁아지면 자연스럽게 줄바꿈됩니다(모바일 3줄).
- 스팟 설명문은 가이드 모달의 '스팟 특징'과 중복이라 제거했습니다.
- 라이브캠은 아이콘 버튼으로 축소(해당 스팟에 URL 이 있을 때만).
- 히어로 숫자 40px → 판정 라벨 16px + 점수 11px. 화면에서 가장 큰 숫자는 이제
  16일 스트립의 파고입니다.

## V.3 배치 변경

```
16일 스트립
지역 컨디션 바     ← 스트립 바로 다음 (스크롤 없이 읽힘)
지도               ← 전체 폭, 높이 478 → 400px
시간별 예보
물때
```

지도+컨디션 2열 그리드를 풀고 전부 전체 폭으로 세로 스택했습니다. 모바일 844px 한 화면에
**스트립 + 컨디션 + 지도 + 시간별 예보 시작**까지 들어옵니다.

## V.4 문구 모순 수정

`energyAdvice()` 와 `verdictReason()` 이 에너지만 보고 `'파도 없음'` 을 먼저 반환해서,
**바람이 완벽해 판정이 '평범 53'인 날에 "평범 · 파도 없음"** 이라는 모순된 문구가 나왔습니다.
(0.4m/6초 = 4.7kJ 인데 글래시라 스코어가 53)

→ `verdictReason()` 은 판정 등급을 먼저 보고 그 안에서 이유를 고릅니다
(`'아주 작지만 깔끔'`). `energyAdvice()` 의 최하 구간은 `'아주 약함'` 으로 바꿨습니다.
종합 판정은 verdict 가, 에너지 라벨은 에너지만 말합니다.

## V.5 알려진 한계

`isToday` 는 **예보를 받은 시점**에 계산됩니다. 페이지를 열어 둔 채 자정을 넘기면
'오늘' 표시가 하루 어긋난 채로 남습니다(새로고침하면 바로 맞습니다). 자정 타이머나
주기적 재요청을 넣을지는 결정이 필요합니다.

---

# [3차-b] W. 날씨 정보 · 레이아웃 압축 · 테이블 버그

## W.1 날씨 데이터 추가

Open-Meteo forecast API 에 `weather_code` · `temperature_2m` · `precipitation_probability` 를
추가로 요청합니다 (16일 384시간 전부 응답 확인).

`src/utils/weather.ts` (신규) — WMO 4677 코드를 한글 라벨 + lucide 아이콘 + 색으로 매핑합니다.
맑음 / 대체로 맑음 / 구름 조금 / 흐림 / 안개 / 이슬비 / 비 / 소나기 / 눈 / 뇌우.

**하루 대표 날씨(`representativeWeatherCode`)는 최빈값이 아닙니다.** 그냥 최빈값을 쓰면
"오후에 소나기 60%"인 날이 '맑음'으로 표시돼 버립니다. 낮 시간대에서 강수 확률이 40% 이상인
시각이 있으면 그 시각의 코드를, 아니면 최빈값을 씁니다. 새벽 비로 하루가 '비'가 되지 않도록
집계 범위는 `SURFABLE_HOURS`(05~20시)로 제한합니다.

| 위치 | 표시 |
|---|---|
| 16일 스트립 | 날씨 아이콘 + 최저/최고 기온 + 강수확률(20% 이상일 때만) |
| 시간별 예보 | **날씨 / 기온 / 강수 확률** 3개 행 추가. 강수확률은 값에 비례한 파란 농도로 배경을 깔아 비 오는 시간대가 한눈에 보입니다 |

## W.2 스트립 카드 압축 — 모바일 세로 우선

"좋음/나쁨" 판정이 배경 있는 블록으로 한 행을 통째로 먹고 있었습니다. 날짜 줄로 합쳐
한 행을 줄였고, 카드 폭도 118 → 104px 로 줄였습니다.

```
[오늘 7/25        ●나쁨]   ← 날짜 + 판정 (합침)
[0.2–0.3m           ☀️]   ← 파고 + 날씨
[26°/27°           58%]   ← 기온 + 강수확률
[아침][낮][오후]           ← 다이얼 + 판정 바
```

파고 범위(`0.2–0.3`)가 104px 안에서 두 줄로 접히길래 판정을 위로 올리고 파고 줄에는
날씨 아이콘만 남겼습니다. 카드 높이 약 30% 감소.

## W.3 🐛 시간별 표의 sticky 열 누출 수정

**증상:** 좌우로 스크롤하면 셀들이 왼쪽 고정 항목열 옆으로 삐져나와 통과해 보였습니다.

**원인:** 스크롤 컨테이너에 `-mx-6 px-6` 이 걸려 있었습니다. `position: sticky; left: 0` 은
**스크롤 컨테이너 경계** 기준이라 sticky 열이 왼쪽 끝에 붙는데, 콘텐츠는 padding 24px
안쪽에서 시작합니다. 그 24px 구간이 sticky 열에 덮이지 않아 다른 셀이 그대로 지나갔습니다.

**수정:** 스크롤 컨테이너에서 좌우 패딩을 제거하고, 그 여백을 첫 열 자체(`pl-6`)가 갖게
했습니다. sticky 셀에는 불투명 배경 + 오른쪽 그림자(`1px 0 0 line, 6px 0 8px -6px`)를 줘
경계를 분명히 했습니다. z-index 도 10 → 20.

> **주의:** 이 표를 다시 손볼 때 스크롤 컨테이너에 좌우 padding 을 넣으면 버그가 그대로
> 재발합니다. 파일 상단 주석에도 남겨 뒀습니다.

## W.4 탭 제거

'시간별 예보 / 물때' 탭을 없애고 **세로로 이어 붙였습니다.** 탭에 숨기면 한 번에 볼 수 있는
정보량이 줄어듭니다. 상세 탭 상태(`detailTab`)도 함께 제거됐습니다.

---

# [2차] A. 테마 시스템 — 밝은 테마 3종

다크 일변도가 너무 어둡다는 피드백을 받아 **밝은 테마를 기본**으로 바꾸고 3종을 옵션으로
제공합니다. 헤더 우측 팔레트 버튼에서 전환하고 `localStorage['k-surf-theme']` 에 저장됩니다.

| 테마 | 성격 | 기본 |
|---|---|---|
| **Sea Glass** | 아침 바다. 시원한 화이트 + 오션 블루 | ✅ |
| **Golden Hour** | 해질녘 세션. 따뜻한 모래빛 + 앰버 | |
| **Night Swell** | 새벽 다크 아웃. 네이비 + 시안 (기존 다크의 후속) | |

theme-factory 스킬의 Ocean Depths / Sunset Boulevard / Arctic Frost 를 참고했지만,
그 파일들은 **프레젠테이션 덱 기준**(짙은 배경 + DejaVu 폰트)이라 웹 UI 대비 기준을
만족하지 않습니다. 스킬의 "Create your Own Theme" 경로를 따라 웹용으로 재설계했습니다.

## A.1 구현 위치

- **색값은 `src/index.css` 의 `[data-theme]` 블록 3개에만** 있습니다. 다른 어디에도 없습니다.
- `src/utils/theme.ts` — 레지스트리·저장·적용. 색값이 아니라 메타데이터(이름·설명·지도 타일)만.
- `src/components/ThemeSwitcher.tsx` — 헤더 드롭다운.
- 지도 타일도 테마를 따라갑니다(`light_all` ↔ `dark_all`). 앱은 밝은데 지도만 어두우면
  화면이 두 동강 납니다.

**새 테마를 추가하려면:** `index.css` 에 `[data-theme='...']` 블록 하나 + `theme.ts` 의
`THEMES` 배열에 한 항목. 컴포넌트는 손댈 필요 없습니다.

## A.2 대비는 전부 계산으로 검증했습니다

3개 테마 × 3개 표면(surface / raised / raised-hi)에서 본문 잉크·액센트가 **WCAG AA 4.5:1**,
마크 전용 토큰이 3:1 을 넘는 것을 dataviz validator 로 확인했습니다.

스코어 순차 램프는 테마별로 ordinal 검사(명도 단조 / 인접 ΔL ≥ 0.06 / 밝은 끝 ≥ 2:1 /
단일 색상)를 통과합니다:

```bash
# Sea Glass
node scripts/validate_palette.js "#8AB6C8,#66A2BB,#3F83A3,#1D688B,#08506F" \
     --ordinal --mode light --surface "#FFFFFF"
# Golden Hour
node scripts/validate_palette.js "#D9A76A,#C38C4C,#A96E31,#89511C,#663B10" \
     --ordinal --mode light --surface "#FFFCF6"
# Night Swell
node scripts/validate_palette.js "#2F5B76,#3E7796,#5194B4,#6FB2D0,#96CFE7" \
     --ordinal --mode dark --surface "#141E2B"
```

**토큰을 바꾸면 반드시 이 명령들을 다시 돌려주세요.**

## A.3 ⚠️ 텍스트 색과 마크 색은 다릅니다 — 헷갈리기 쉬운 지점

밝은 테마에서 AA(4.5:1)를 만족하는 색은 어두워서, 그대로 막대에 칠하면 탁해 보입니다.
반대로 예쁜 밝은 색은 텍스트로 쓰면 대비가 모자랍니다. 그래서 판정색을 **둘로 나눴습니다**:

```
--good / --fair / --poor         → 텍스트용 (4.5:1)
--good-fill / --fair-fill / ...  → 막대·아이콘용 (3:1). 텍스트에 쓰지 말 것
--good-soft / ...                → 옅은 배경
```

코드에서는 `verdict.colorVar`(텍스트) / `verdict.fillVar`(마크)로 구분합니다.

---

# [2차] ~~B. 7일 파도 스트립~~ → **[3차] §Y 의 16일 스트립으로 대체됨**

> 판정 3채널(색+도형+라벨)과 아침/낮/오후 분해는 그대로 유지됩니다. 아래는 그 근거 기록입니다.
> **단, 풍향 화살표 설명은 [3차] §X 에서 해변 단면 다이어그램으로 완전히 교체됐습니다.**

첨부해 주신 Surfline 10일 스트립을 참고하되, **더 직관적으로** 만들라는 요구에 맞춰
세 가지를 더했습니다.

### B.1 색만으로 말하지 않습니다
Surfline 은 주황/분홍 막대뿐이라 색을 모르면 좋고 나쁨을 알 수 없습니다.
여기서는 판정이 **세 채널**로 동시에 나갑니다:

| 판정 | 색 | 도형 | 라벨 |
|---|---|---|---|
| **좋음** (60점~) | good | 꽉 찬 원 ● | "좋음" |
| **평범** (38~59) | fair | 마름모 ◆ | "평범" |
| **나쁨** (~37) | poor | 납작한 막대 ▬ | "나쁨" |

색맹이거나 흑백으로 출력해도 도형과 한글 라벨이 남습니다.

### B.2 하루를 아침/낮/오후로 쪼갭니다
서핑은 **같은 날에도 오전 육풍(오프쇼어)과 오후 해풍(온쇼어)으로 컨디션이 완전히 갈립니다.**
하루를 한 값으로 뭉개면 그 정보가 사라집니다. 각 구간마다:
- **풍향 화살표** — 회전각이 방향, **채워진 화살표 = 오프쇼어**(파도가 세워짐)
- **판정 세그먼트** — 그 시간대의 좋음/평범/나쁨

풍향 화살표는 기상 풍향에 180°를 더해 **바람이 흘러가는 방향**을 그립니다
(`windArrowRotation()`). 기상 풍향은 "불어오는 방향"이라 그대로 그리면 반대가 됩니다.

### B.3 베스트를 직접 찍어 줍니다
"이번 주 최고" 배지 + 헤더에 한 문장 요약("일요일 오후가 이번 주 최고입니다").
훑어보고 스스로 찾게 하지 않습니다.
단, **최고점이 '나쁨' 구간이면 배지를 달지 않습니다.** 나쁜 날들 중 1등은 추천이 아닙니다.

---

# [2차] C. 레이아웃 — 지도와 파도를 한 화면에서

```
1행)  지도 (좌 1.4fr)  |  지금 컨디션 (우 1fr)
2행)  오늘 포함 7일 스트립
3행)  상세 탭 — 24시간 / 물때 / 2주 전망
```

예전에는 지도 아래로 배너·카드·표가 세로로 길게 쌓여서 **지도와 예보를 동시에 볼 수
없었습니다.** 이제 첫 화면 안에 지도 + 현재 컨디션 + 7일이 모두 들어옵니다.

부수 변경:
- `GoldWaveBanner.tsx` **삭제** — 히어로 수치가 SpotHeader 와 중복이었고, 역할(오늘 피크
  타임)은 SpotHeader 안의 한 줄로 흡수했습니다.
- 24시간 표 / 물때 차트 / 2주 전망은 탭 뒤로 접었습니다. 셋 다 살아 있습니다.
- 지도 마커에 **오늘 판정색 점**이 붙습니다. 예보를 스팟 단위로 받으므로 선택된 스팟만
  판정색이고 나머지는 중립색입니다("아직 모름"을 정직하게 표시).

---

# [2차] D. 🔴 스코어 엔진을 재보정했습니다 — 확인 필요

1차 핸드오프 §5.1 에서 "제품 결정이라 손대지 않았다"고 남겼던 항목입니다. 이번에
**좋음/평범/나쁨 3단계 표시**를 요구받았는데, 기존 임계값으로는 동해안 여름이 7일 내내
'나쁨' 하나로만 나와 기능 자체가 무의미해집니다. 그래서 **보정하고 여기에 명시합니다.**
되돌리거나 다시 조정하려면 `src/utils/surfScoreEngine.ts` 한 파일만 보면 됩니다.

### 바뀐 값

| | 이전 | 지금 | 이유 |
|---|---|---|---|
| FLAT 게이트 | `< 0.25m` 또는 `< 15kJ` | `< 0.2m` 또는 `< 4kJ` | 0.6m/7초(12.3kJ)는 한국에서 롱보드로 충분히 타는 가장 흔한 컨디션인데 FLAT 처리됐음 |
| 에너지 구간 | 450 / 250 / 120 / 50 | 150 / 80 / 35 / 15 / 6 | 하와이·인니 기준 → 한국 실제 분포(약 5~200kJ) |
| 주기 구간 | 9 / 7 / 5 | 9 / 7 / 6 / 5 | 한국은 9초 이상이 드묾. 6~7초대를 "쓸 만함"으로 인정 |

참고 환산: `0.6m/6s≈11kJ · 0.8m/7s≈22kJ · 1.0m/8s≈39kJ · 1.5m/9s≈99kJ · 2.0m/10s≈196kJ`

### 효과 (실측)
- **이전:** 양양 38선 14일 전부 `10점 / 플랫` → 카드가 전부 똑같아 판단 불가
- **지금:** 오늘 나쁨(6) · 일 평범(50) · 월 평범(50) · 화 평범(50) · 수~금 나쁨(20~32)
  → 주말에 가야 한다는 게 한눈에 읽힘

### 판정 임계값은 별도 파일입니다
`src/utils/scoreVisuals.ts` 의 `verdictOf()` 에 `60 / 38` 로 있습니다.
**"좋음이 너무 후하다/박하다" 같은 조정은 엔진이 아니라 이 함수만 고치면 됩니다.**

---

# [1차] 이하 — 최초 점검 및 디자인 개편 기록

## 0. 한 줄 요약

빌드·타입체크는 원래도 통과했지만, **화면의 상당 부분이 글자가 안 보이는 상태**였고
(밝은 테마용 색이 다크 셸 위에 얹혀 있었음), **물때 차트는 상수를 그리고 있었으며**,
**권역 필터는 아무것도 하지 않는 죽은 UI** 였습니다. 셋 다 고쳤고, 디자인 시스템을
토큰 기반으로 다시 세웠습니다.

검증: `npm run build` 통과, `npx tsc --noEmit` 클린, 런타임 콘솔 에러 0.
데스크톱(1280) / 모바일(390) 실제 렌더 확인 완료.

---

## 1. 가장 중요한 발견 — 테마가 반쯤만 뒤집혀 있었습니다

앱은 원래 **밝은 "Golden Hour" 테마**(크림색 카드 `#FFFDF9` + 짙은 갈색 글자 `#4A403A`)로
작성됐는데, 어느 시점에 셸만 **Chocolate Brown 다크**(`#4A403A` 배경)로 바뀌었습니다.
컴포넌트 내부 색은 그대로 남아서 이런 상태였습니다.

| 컴포넌트 | 증상 |
|---|---|
| `SpotHeader` | 스팟 이름·파고·에너지·풍속 **전부** `text-[#4A403A]` = 배경과 같은 색. 사실상 안 보임 |
| `HourlyForecastTable` | 본문 셀 전부 동일 증상. 표가 통째로 비어 보임 |
| `TideChart` | 제목·수치 안 보임 |
| `Forecast16DaysTable`, `GoldWaveBanner`, `SpotGuideModal` | 반대로 밝은 카드가 그대로 남아, 한 화면에 라이트/다크가 섞임 |

**대응:** 다크 하나로 확정하고, 색을 CSS 변수 한 곳(`src/index.css`)에 모았습니다.
컴포넌트에서 하드코딩 hex 를 전부 걷어냈습니다.

---

## 2. 새 디자인 시스템

### 2.1 토큰 (`src/index.css` → `tailwind.config.js`)

색은 이제 **전부** `src/index.css` 의 `:root` 에서만 정의되고, Tailwind 는 그걸 시맨틱
이름으로 노출합니다. 테마를 바꾸려면 `:root` 만 건드리면 됩니다.

```
표면  --bg #221C19 · --surface #2C2521 · --raised #382F2A · --raised-hi #443932
잉크  --ink #F7F0E6 · --ink-2 #CBB9A6 · --ink-3 #A8968A · --ink-mark #8B7A6D(마크 전용)
액센트 --gold #F4A900(에너지/브랜드) · --tide #5BD1C4(물) · --rose #E8848A(바람/경고)
```

> **컴포넌트에서 `bg-[#3B332E]` 같은 리터럴 hex 를 다시 쓰지 말아 주세요.**
> `bg-surface` / `text-ink-2` / `border-line-soft` 를 씁니다.
> `--ink-mark` 는 대비 3.2:1 이라 **본문 텍스트로 쓰면 안 됩니다** (아이콘·눈금 전용).

### 2.2 대비는 눈으로 고르지 않고 계산했습니다

dataviz 스킬의 validator 로 검증했습니다. surface `#2C2521` 기준:

| | 대비 | |
|---|---|---|
| `--ink` | 13.3:1 | 제목·수치 |
| `--ink-2` | 7.9:1 | 본문 |
| `--ink-3` | 5.3:1 | 캡션 |
| `--gold` | 7.6:1 | |
| `--tide` | 8.2:1 | |
| `--rose` | 5.8:1 | |

전부 WCAG AA(4.5:1) 통과입니다.

### 2.3 서프 스코어 색 램프

스코어(0~100)는 **단일 색상 순차 램프** 5단계입니다. validator 의 ordinal 검사를 통과했습니다
(명도 단조 증가 / 인접 ΔL ≥ 0.06 / 어두운 끝 2.50:1 / 색상 폭 4°).

```bash
node scripts/validate_palette.js "#75603A,#96773A,#B79038,#D8AA3C,#F9C858" \
     --ordinal --mode dark --surface "#2C2521"
# → ALL CHECKS PASS
```

램프를 바꾸면 **반드시 이 명령을 다시 돌려주세요.** 값은 `--score-1` … `--score-5`.

### 2.4 표현 규칙을 한 곳으로 — `src/utils/scoreVisuals.ts` (신규)

이전에는 각 컴포넌트가 임계값(75/50/30…)과 색을 제각각 인라인으로 들고 있어서,
**같은 파도가 화면마다 다른 색**으로 보였습니다. 이제 여기서만 정의합니다.

- `scoreBucket()` / `scoreColorVar()` / `scoreLabel()` / `scoreFillStyle()`
- `windMeta()` — 바람 타입의 짧은 라벨·긴 라벨·색
- `energyAdvice()` — kJ → 보드 추천 문구

**스코어 임계값이나 색을 바꿀 일이 생기면 이 파일만 고치면 됩니다.**

### 2.5 타이포그래피

- 기존에는 거의 모든 요소가 `font-black`/`font-extrabold` 이었습니다. 전부 굵으면 위계가
  사라집니다. **크기와 색으로 위계를 만들고 굵기는 semibold 까지만** 쓰도록 정리했습니다.
- 폰트 스택 첫 항목이 `FreeSans`(리눅스 폰트)였고, Inter 는 **한글 글리프가 없습니다.**
  → `index.html` 에 **Pretendard** 를 추가하고 스택을 `Inter, Pretendard, system-ui…` 로 정리.
- 표·눈금의 숫자에만 `tabular-nums` 적용(`.num` 클래스 / `table td`, `table th`).
  히어로 숫자에는 일부러 넣지 않았습니다(큰 크기에서 자간이 벌어져 보임).

### 2.6 히어로 수치는 화면당 하나

`GoldWaveBanner` 와 `SpotHeader` 가 둘 다 큰 스코어 숫자를 들고 있었습니다.
→ **`SpotHeader` 가 히어로(52px 스코어 + 미터)를 소유**하고,
`GoldWaveBanner` 는 "언제 들어가야 하는가"(피크 타임)만 말하는 슬림 스트립으로 바꿨습니다.

---

## 3. 고친 기능 버그

### 3.1 🔴 물때가 상수였습니다 — `src/services/surfApi.ts`

```ts
tideHeightCm: 45,      // ← 모든 시각에 하드코딩
tideState: 'HIGH',
```

`TideChart` 가 **항상 완전한 직선**을 그리고 있었고, 24시간 표의 물때 행도 전부 `45cm` 였습니다.

**대응:** Open-Meteo marine API 에 `sea_level_height_msl`(평균해수면 대비 조위, m)를 추가로
요청해서 실제 값을 씁니다. 확인된 실측 예(제주 중문, 2026-07-25):
간조 31cm(01시) → 만조 53cm(06시) → 간조 -40cm(13시) → 만조 108cm(20시).

### 3.2 만조/간조 판정 — 평평한 구간 오분류

조위가 평평한 구간(예: `0.12, 0.12, 0.12`)에서 앞뒤 값만 비교하면 여러 시각이 동시에
만조로 잡혀서 **간조 없이 만조가 두 번 표시**되는 물리적으로 불가능한 결과가 났습니다.
→ `classifyTide()` 가 ±2시간 창까지 보고 실제 극점에서만 HIGH/LOW 를 반환합니다.
`TideChart` 쪽에도 라벨 충돌 방지용 dedupe 를 이중으로 뒀습니다.

### 3.3 🔴 권역 필터가 죽은 UI 였습니다

`selectedRegion` 상태는 있었지만 **아무 데도 쓰이지 않았습니다.** 탭을 눌러도 목록·지도
어느 쪽도 바뀌지 않았습니다.
→ `App` 에서 `visibleSpots` 를 만들어 헤더 셀렉트와 지도 마커가 **같은 목록**을 쓰게 했고,
필터 때문에 선택 스팟이 목록 밖으로 나가면 자동 보정합니다. 탭에 스팟 수도 표시합니다.

### 3.4 지도 마커가 선택 상태를 반영하지 않았습니다

마커 생성 `useEffect` 의 의존성 배열이 `[]` 여서, 스팟을 바꿔도 마커의 "선택됨" 스타일이
영영 갱신되지 않았습니다(팝업만 이동). → 마커 갱신을 별도 이펙트로 분리.

### 3.5 API 실패 시 화면이 통째로 사라졌습니다

`generateFallback16Days()` 가 `hourly: []` 를 돌려줘서, 네트워크가 끊기면 `activeForecast`
가 null → 지도 아래가 전부 사라졌습니다.
→ 폴백이 실제 시간별 데이터를 생성합니다(동해안의 전형적 일주기 — 오전 육풍/오후 해풍,
반일주조 근사 조석). `isLiveApi: false` 로 표시되고 UI 에 **"오프라인 추정치"** 배지가 뜹니다.

### 3.6 `isLiveApi` 가 한 번도 세팅되지 않았습니다

타입에는 있는데 서비스가 채우지 않아서 **"● Open-Meteo 실시간" 배지가 영영 안 떴습니다.**
→ 실시간 경로 `true`, 폴백 경로 `false`.

### 3.7 Tailwind v4 클래스가 v3 프로젝트에 섞여 있었습니다 (조용히 무시됨)

프로젝트는 `tailwindcss ^3.4.1` 인데 아래는 v4 이름이라 **아무 효과가 없었습니다**:

| 클래스 | 사용 횟수 | |
|---|---|---|
| `shadow-xs` | 11 | v3 에는 없음 |
| `backdrop-blur-xs` | 1 | v3 는 `-sm` |
| `border-l-3` | 1 | 존재하지 않는 값 |
| `py-0.2` | 2 | 존재하지 않는 값 |
| `no-scrollbar` | 4 | **어디에도 정의된 적 없음** → 스크롤바가 계속 보였음 |
| `animate-fadeIn` | 1 | **정의된 적 없음** |

→ 전부 정리했고, `no-scrollbar` / `fade-edge-x` / `animate-fadeIn` / `animate-riseIn` 을
실제로 정의했습니다.

### 3.8 잘못된 표 마크업

`<thead><tr>` 안에 `<td>` 가 들어가 있었고, `<th>` 에 `display:flex` 가 걸려 셀 정렬이
깨져 있었습니다. → `<th scope="col">` / `<th scope="row">` 로 바로잡고 flex 는 내부 span 으로.

### 3.9 기타

- `timezone=Asia%2FTokyo` → `Asia/Seoul` (오프셋은 같지만 한국 앱에 도쿄는 부적절)
- 양양 38선의 `liveCamUrl` 이 깨진 문자열(`%C9%C3%C3%C3…`)이라 **링크가 동작하지 않았습니다.**
  → 제거. **(§5 참고 — 라이브캠은 사실상 미구현입니다)**
- `SpotMapView` 의 미사용 import(`Waves`, `Zap`, `Wind`) 제거
- 지도 타일을 `voyager`(밝음) → `dark_all` 로 교체. 앱은 다크인데 지도만 밝아 화면이 두 동강
  나 있었습니다. Leaflet 팝업·줌 컨트롤·attribution 도 다크로 오버라이드.
- 지도 카메라: 기존에는 선택 스팟에 zoom 10 으로 붙어 있어 **나머지 6개 스팟이 화면 밖**
  이었습니다(= Map-First 인데 스팟이 하나만 보임). 이제 표시 중인 스팟이 모두 들어오게
  `fitBounds` 합니다. 정수 줌만 쓰면 한반도가 세로로 겨우 안 들어가 한 단계가 통째로
  빠지면서 중국·일본까지 노출되길래 `zoomSnap: 0.25` 로 소수 줌을 허용했습니다.
- 지도에서 `scrollWheelZoom: false` — 페이지 스크롤을 가로채지 않도록.
- 모달에 ESC 닫기 / 배경 클릭 닫기 / `role="dialog"` 추가.
- 로딩 상태를 스피너 → 레이아웃과 같은 모양의 스켈레톤으로. 로드 전후 화면이 튀지 않습니다.
- `prefers-reduced-motion` 대응 추가.

---

## 4. 데이터 시각화 관련해서 바꾼 것 (의도가 있는 변경입니다)

**14일 스파크바의 정규화 기준을 관측 최댓값 → 고정 도메인(0~100)으로 바꿨습니다.**
관측 최댓값으로 정규화하면 2주 내내 플랫인 주간(현재 동해안 7월이 그렇습니다)도 막대가
꽉 차 보여서 **크기를 잘못 말합니다**. 지금은 플랫한 주는 낮고 평평하게 그려집니다.

색만으로 의미를 전달하는 곳이 없도록, 스코어에는 **항상 숫자와 단계 라벨**
(플랫/약함/보통/좋음/최상)을 함께 붙였고 24시간 표 하단에 범례를 넣었습니다.

물때 차트는 호버 크로스헤어 + 툴팁을 붙였고, 직접 라벨은 **만조/간조 극점에만** 답니다
(모든 점에 숫자를 붙이지 않습니다).

---

## 5. ⚠️ 남아 있는 것 — Antigravity 의 판단이 필요합니다

### 5.1 ~~🔴 스코어 엔진이 한국 여름 바다에 맞지 않습니다~~ → **[2차] §D 에서 해결됨**

> 아래는 1차 시점의 기록입니다. 2차 작업에서 재보정했고 근거·수치는 **[2차] §D** 에 있습니다.

`src/utils/surfScoreEngine.ts` 의 FLAT 게이트:

```ts
if (waveHeightM < 0.25 || energyKJ < 15) → score 10, FLAT
```

`0.6m / 7초` = 4.9 × 0.36 × 7 ≈ **12.3kJ → FLAT**. 그런데 0.6m/7초는 한국에서 롱보드로
충분히 타는, 오히려 **가장 흔한 컨디션**입니다.

실제로 오늘(7/25) 양양 38선은 **14일 전부 `10점 / 플랫`** 으로 나옵니다. 카드 14장이
전부 똑같아서 "어느 날 갈까"를 판단할 수 없습니다 — 이 앱의 핵심 가치가 사라집니다.
(제주 중문은 63점이 나와서 대비가 보입니다. 즉 엔진이 망가진 게 아니라 **임계값이
한국 동해 여름 기준으로 너무 높습니다**.)

**제안:** FLAT 게이트를 `energyKJ < 6` 수준으로 낮추고, 에너지 구간
(450/250/120/50)도 한국 기준으로 재조정. **다만 이건 제품 정의라 제가 임의로 바꾸지
않았습니다.** 결정해 주시면 반영하겠습니다.

관련해서 함께 볼 것:
- `starCount` 는 0~10 인데 UI 는 `7★` 로 표시했었습니다(별 10개 스케일은 관례가 아님).
  지금은 별점 대신 스코어+라벨로 통일했고 `starCount` 는 **UI 에서 쓰이지 않습니다.**
  타입에는 남아 있으니 정리하거나 다시 쓰거나 결정이 필요합니다.
- `SpotHeader` 에 `Surf Score (10 Max)` 라고 써 있는데 값은 `/100` 이었습니다(수정됨).

### 5.2 라이브캠은 사실상 미구현입니다

`liveCamUrl` 이 있는 스팟은 양양 38선 하나뿐이었고 그 URL 이 깨져 있었습니다.
죽도는 `liveCamTitle` 만 있고 URL 이 없어 버튼이 렌더되지 않습니다.
→ **7개 스팟 중 실제로 동작하는 라이브캠이 0개입니다.** 검증된 URL 을 채우거나
기능 자체를 제거할지 결정이 필요합니다. (UI 코드는 URL 이 있으면 뜨도록 남겨뒀습니다.)

### 5.3 이름과 실제가 어긋난 것들

- `fetchLive16DaysForecasts`, `DailyForecast14Days`, `Forecast16DaysTable` —
  **16 과 14 가 섞여 있고** 실제 요청은 `forecast_days=14` 입니다.
  참고로 Open-Meteo marine 은 `forecast_days=16` 도 200 을 반환합니다(확인함).
  16일로 갈지 14일로 통일할지 정하고 이름을 맞추는 게 좋겠습니다.
- 파일명 `Forecast16DaysTable.tsx` 는 이제 테이블이 아니라 카드 그리드입니다.

### 5.4 [2차] 새로 생긴 확인 사항

- **판정 임계값(60/38)은 한 번 감으로 정한 값입니다.** 실제 서퍼 피드백으로 조정하세요.
  `src/utils/scoreVisuals.ts` 의 `verdictOf()` 한 곳입니다.
- **7일 스트립의 판정은 그날 "최고" 점수 기준**입니다(하루 중 가장 좋은 시간 기준).
  평균으로 바꾸고 싶으면 `summarizeDay()` 의 `maxScore` 를 보세요.
- **다른 스팟의 오늘 판정은 지도에 표시되지 않습니다.** 예보를 스팟 단위로 받기 때문입니다.
  7개 전부를 지도에 색으로 물들이려면 스팟별 예보를 병렬로 받아 캐시해야 합니다
  (Open-Meteo 는 여러 좌표를 `latitude=a,b,c` 로 한 번에 받는 것을 지원하므로
  요청 1회로 가능합니다). 꽤 좋은 다음 스텝이라고 봅니다.
- 테마 전환 시 지도 타일 레이어를 교체하므로 **타일이 잠깐 다시 로드**됩니다. 거슬리면
  두 레이어를 미리 만들어 두고 opacity 로 크로스페이드하면 됩니다.

### 5.5 기타 낮은 우선순위

- `sufline.xapk`, `surfline_files/` 가 레포에 들어 있습니다. 벤치마킹 자료라면 `.gitignore`
  로 빼는 게 좋겠습니다(용량·라이선스).
- 테스트가 하나도 없습니다. 최소한 `surfScoreEngine` 과 `classifyTide` 는 순수 함수라
  단위 테스트를 붙이기 쉽습니다.
- 에러 바운더리가 없습니다.
- `.claude/launch.json` 을 추가했습니다(dev 서버 실행용, 포트 3050).

---

## 6. 변경 파일

| 파일 | |
|---|---|
| `src/index.css` | **재작성** — 디자인 토큰 + 프리미티브(`.panel`/`.card`/`.segmented`/`.badge`/`.btn`/`.skeleton`) + 누락 유틸 + Leaflet 다크 오버라이드 |
| `tailwind.config.js` | **재작성** — 시맨틱 색 토큰, 폰트 스택, 애니메이션 |
| `index.html` | Pretendard 추가, 메타 정리, `color-scheme: dark` |
| `src/utils/scoreVisuals.ts` | **신규** — 표현 규칙 단일 소스 |
| `src/services/surfApi.ts` | 조위 실데이터, `classifyTide()`, `isLiveApi`, 타임존, 폴백 재작성, `describeDay()` |
| `src/App.tsx` | 권역 필터 배선, 스켈레톤, 에러 상태 |
| `src/components/Header.tsx` | 재작성 — 필터 실동작 + 카운트 |
| `src/components/SpotHeader.tsx` | 재작성 — 히어로 + 3 스탯 타일 |
| `src/components/HourlyForecastTable.tsx` | 재작성 — 가독성 + 유효 마크업 + 히트맵/범례 |
| `src/components/Forecast16DaysTable.tsx` | 재작성 — 스파크바 + 스코어 카드 |
| `src/components/TideChart.tsx` | 재작성 — 실데이터 + 호버 + 극점 라벨 |
| `src/components/GoldWaveBanner.tsx` | 재작성 — 슬림 스트립 |
| `src/components/SpotGuideModal.tsx` | 재작성 — 다크 + a11y |
| `src/components/SpotMapView.tsx` | 재작성 — 다크 타일, 마커 갱신, fitBounds, 필터 |
| `.claude/launch.json` | 신규 |
| `HANDOFF.md` | 이 문서 |

### [2차] 추가 변경

| 파일 | |
|---|---|
| `src/index.css` | **재작성** — `[data-theme]` 3종 + 텍스트/마크 분리 토큰 |
| `src/utils/theme.ts` | **신규** — 테마 레지스트리·저장·적용 |
| `src/components/ThemeSwitcher.tsx` | **신규** — 헤더 테마 드롭다운 |
| `src/components/WeekStrip.tsx` | **신규** — 7일 스트립 (핵심 화면) |
| `src/utils/surfScoreEngine.ts` | **재보정** — 한국 해역 기준 (§D) |
| `src/utils/scoreVisuals.ts` | 3단계 판정(`verdictOf`) + `windArrowRotation` 추가 |
| `src/types/surf.ts` | `DayPart` · `isToday` · `minWaveHeightM` · `parts` 추가 |
| `src/services/surfApi.ts` | `summarizeDay()` — 아침/낮/오후 구간 요약 (실시간·폴백 공용) |
| `src/App.tsx` | 지도+컨디션 2열 → 7일 스트립 → 상세 탭 |
| `src/components/SpotHeader.tsx` | 우측 컬럼용 세로 카드로 재구성, 피크타임 흡수 |
| `src/components/SpotMapView.tsx` | 테마별 타일, 마커에 오늘 판정색 |
| `src/components/GoldWaveBanner.tsx` | **삭제** — SpotHeader 로 흡수 |
| `tailwind.config.js` | good/fair/poor · brand 토큰 추가 |

`src/types/surf.ts` 와 `src/utils/surfScoreEngine.ts` 는 **건드리지 않았습니다**
(§5.1 의 판단이 필요해서).

---

## 7. 실행

```bash
npm run dev
```

```bash
npm run build
```
